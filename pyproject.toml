[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "flowstorm"
version = "0.1.0"
description = "Normalizing-flow-based detector systematics reweighting."
requires-python = ">=3.9"